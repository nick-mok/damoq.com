import Layout from '../../components/MyLayout';

export const Movie = (props) => {

    return(
        <Layout pageTitle="Movie">
            <h1>Movie Name</h1>
            <p>The movie description</p>
        </Layout>
    );
}

export default Movie;