import Layout from '../components/MyLayout';

export const Comms = (props) => {

    return (
        <Layout pageTitle="Commendations">
            <h1>Comming Soon</h1>
        </Layout>
    );
}

export default Comms;